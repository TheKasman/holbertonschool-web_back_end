#!/usr/bin/env python3
"""2 to the floor, i was sure...? doesn't sound right"""
import math

def floor(n: float) -> int:
    """Returns the floor of a float"""
    print(repr(math.floor(n)))
    return math.floor(n)
